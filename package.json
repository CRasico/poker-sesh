{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "3.22.1"
  },
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start --parallel",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint",
    "format": "lerna run format"
  },
  "dependencies": {
    "typescript": "^4.8.3"
  }
}

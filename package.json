{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "build": "lerna run build",
    "start": "lerna run start",
    "test": "lerna run test",
    "bootstrap": "lerna bootstrap",
    "lint": "lerna run lint",
    "format": "lerna run format"
  },
  "dependencies": {
    "typescript": "^4.8.3"
  }
}
